// ITP (예: 677점 만점) 를 TOEFL IBT (120만점) 로 변환하는 함수
export function convertToeicToIbt(toeicScore: number): number {
  // TODO: 여기에 실제 변환 공식을 구현하세요.
  // 예시(단순 비례환산): return (toeicScore / 677) * 120;
  return toeicScore;
}

// IELTS (예: 9.0 만점) 를 TOEFL IBT (120만점) 로 변환하는 함수
export function convertIeltsToIbt(ieltsScore: number): number {
  // TODO: 여기에 실제 변환 공식을 구현하세요.
  // 예시(단순 비례환산): return (ieltsScore / 9) * 120;
  return ieltsScore;
}
